// import { FastifyRequest, FastifyReply } from 'fastify';

/**
 * Fastify authentication middleware stub.
 * Always allows access for demonstration purposes.
 * @param request FastifyRequest
 * @param reply FastifyReply
 * @param done Callback to continue
 */

export async function authMiddleware(): Promise<void> {
  // Stub: always allow access
  // In production, implement real authentication logic here
  return;
}
